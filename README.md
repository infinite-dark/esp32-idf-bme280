**An ESP-IDF fully native BME280 I2C driver using the updated I2C API (non-legacy).**

Warning:
- this is a PERSONAL exercise and a work-in-progress,
- this is NOT meant for production use and comes with NO WARRANTY,
- this project is NOT an official IDF component,
- this project is NOT configured for use yet,
- this driver was not meant to be concurrently safe (user's responsibility).

*I did not want to make this repo public, but it was necessary for personal reasons.*
